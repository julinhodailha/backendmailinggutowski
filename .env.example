# ─── Gutowski Mailing System · Variáveis de Ambiente ───────────────────────
# Copie este arquivo para .env e preencha os valores.
# No Railway, configure essas variáveis em Settings > Variables.

# ─── OBRIGATÓRIO EM PRODUÇÃO ─────────────────────────────────────────────────

# Segredo JWT — gere com: openssl rand -hex 64
JWT_SECRET=gutowski_super_secret_change_in_production_2024

# Credenciais do usuário admin
ADMIN_EMAIL=admin@amanda

# Hash bcrypt da senha. Gere com:
#   node -e "const b=require('bcryptjs'); console.log(b.hashSync('SUA_SENHA_AQUI', 10))"
# A senha padrão abaixo é: 2563Amanda@gutowski
ADMIN_HASH=$2a$10$N9qo8uLOickgx2ZMRZoMyeIjZAgcfl7p92ldGxad68LJZdL17lbu2

# ─── OPCIONAL ────────────────────────────────────────────────────────────────
NODE_ENV=production
PORT=3000
